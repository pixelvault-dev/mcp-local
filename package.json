{
  "name": "@pixelvault-dev/local-mcp",
  "version": "0.1.0",
  "description": "Local (stdio) MCP server for PixelVault — upload images by file path, no base64 through the model context",
  "type": "module",
  "bin": {
    "pixelvault-local-mcp": "./bin/pixelvault-local-mcp.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "pixelvault",
    "mcp",
    "model-context-protocol",
    "image-hosting",
    "agent",
    "upload"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pixelvault-dev/mcp-local.git"
  },
  "bugs": {
    "url": "https://github.com/pixelvault-dev/mcp-local/issues"
  },
  "homepage": "https://github.com/pixelvault-dev/mcp-local#readme",
  "author": "PixelVault",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
